# Screenshots

Reserved for task-pane screenshots taken against `test-data/sample_data.csv` (or another workbook built the same way) once the add-in has been manually sideload-tested — see `test-data/README.md`.

Screenshots committed here must show only synthetic data, same rule as everywhere else in this repo (see the root README's "Privacy / synthetic data only" section).
